"use client";

/**
 * DEPRECATED: Use `useTheme` from "next-themes" directly instead.
 *
 * This file exists only for backward compatibility with any imports
 * that reference `@/contexts/ThemeContext`. All theme state is now
 * managed by `next-themes` via the `ThemeProviderWrapper` in layout.tsx.
 */

export { useTheme, ThemeProvider } from "next-themes";
