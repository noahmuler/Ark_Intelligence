Here is the high-precision engineering prompt optimized for Windsurf to rebuild the Market Mood card on your asset Deep Dive page (/macro/[asset]). It integrates the exact semi-circular gauge structure from the reference library while strictly locking in your signature dark purple glassmorphic theme.Engineering & UI Refinement Specification: Market Mood Gauge OverhaulObjective: Completely rewrite the Market Mood component inside the asset Deep Dive page (/macro/[asset]) to achieve exact layout parity with HybridTrader. Replace the current gauge implementation with a highly polished, semi-circular radial arc inspired by the chamaac.com gauge configuration. Clean up legacy text strings, optimize layout density, and integrate our custom dark purple theme.References:Target Git Repository: https://github.com/noahmuler/Ark_Intelligence (Deep Dive / Asset Macro layout sub-files)Gauge Structural Blueprint: https://www.chamaac.com/components/sections/gaugeOriginal App Layout Reference: https://hybridtrader.ai/Please execute the following codebase modifications:1. High-Fidelity Semi-Circular Gauge Component ImplementationThe Structural Blueprint: Replace any full-circle or generic pie-chart widgets with a dedicated semi-circular gauge arc ($180^\circ$ sweep angle, running from $-90^\circ$ to $+90^\circ$ or $180^\circ$ to $0^\circ$ depending on your SVGs/charts framework).The Math & Needle Alignment: * Build a clean, minimalist SVG path track or use a customized radial configuration block via Recharts/Framer Motion.Implement a centralized Needle/Indicator vector that accurately rotates dynamically along the arc based on the real-time sentiment coefficient ($0$ to $100$ or Extreme Fear to Extreme Greed).Theme Styling Overrides: * Do NOT use Chamaac's default green/red spectrum. The gauge tracking arc must remain consistent with the Ark Intelligence visual profile. Use a gradient track shifting from a muted, translucent deep purple (rgba(147, 51, 234, 0.1)) to a vibrant, glowing neon purple accent (rgba(168, 85, 247, 0.8)) for the active value track.The central reading text (e.g., "72") should be large, crisp, and centered directly beneath the pivot point of the needle indicator.2. High-Density Sentiment Metric LayoutSymmetrical Structural Mapping: Re-architect the rest of the Market Mood card to exactly match the metadata alignment of the original.Key-Value Realignment: Organize current sentiment, historical context, and sentiment labels into compact, high-density rows below or adjacent to the gauge track. Use a clean sans-serif typography scale with tight line spacing to avoid unnecessary visual height.3. Codebase Housekeeping & DOM PurgePrune Redundant Elements: Scan the component file and aggressively remove any fallback metric text blocks, unused nested canvas wrappers, or ghost layout containers added during previous alignment iterations.Flatten Container Nesting: Ensure the entire widget uses exactly one outer glassmorphic container layer (backdrop-filter: blur(12px), border: 1px solid rgba(147, 51, 234, 0.15)). Eliminate nested parent blocks that create double background rendering or distort padding lines on desktop viewports.Windsurf Execution Checklist:Is the sentiment gauge configured as a precise, clean semi-circle ($180^\circ$ arc) with a dynamic tracking indicator?Are all default red/green color tokens completely bypassed in favor of our custom glowing purple theme accents?Has the file been audited to eliminate any "ghost" containers or duplicated text labels?Does the visual weight and compact layout density perfectly match the original HybridTrader asset metrics overview?Go.