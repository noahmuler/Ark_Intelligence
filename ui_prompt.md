Here is the refined engineering prompt for Windsurf to fix the missing needle rendering bug, configure the 3-state label mapping, and establish pixel-perfect layout parity with the original structure.Engineering & UI Refinement Specification: Deep Dive Market Mood Card & Needle AlignmentObjective: Fix the missing needle rendering bug and configure the 3-state text logic inside the Market Mood Card on the Asset Deep Dive page (/macro/[asset]). The gauge size has been successfully reduced; now, establish exact visual parity with HybridTrader using a clean, number-free gauge track, structural data columns, and our custom dark purple glassmorphic theme.References:Target Git Repository: https://github.com/noahmuler/Ark_IntelligenceGauge Structural Blueprint: https://www.chamaac.com/components/sections/gaugeOriginal App UI Layout: https://hybridtrader.ai/1. Visual Anatomy of the Original (HybridTrader)The Layout: A tightly packed, asymmetrical two-column layout inside a single premium glassmorphic card.The Left Side (Visual Metric): A clean, minimalist semi-circular gauge ($180^\circ$ arc) stripped entirely of superficial text, tick marks, or numbers. A solid, visible needle stems from the absolute bottom-center coordinate of the arc diameter, dynamically pointing to the current value.The Right Side (Context Definitions): A dense vertical stack displaying the categorical states, current reading, and directional bias implications. It features sharp, high-contrast, color-coded state indicators.2. High-Priority Code Fixes for WindsurfA. Reconstruct and Render the Gauge NeedleThe gauge sizing is correct, but the needle path is failing to mount or calculate its transform anchor. Update the SVG layout or the chamaac component wrapper to explicitly declare the pointer:Anchor Point: Force the needle origin/pivot coordinate (transform-origin) to the exact horizontal middle and vertical bottom of the semi-circle frame ($50\%$ $100\%$).Mathematical Rotation Tracking: Ensure the degree parsing uses a clean linear interpolation based on a $0\text{--}100$ sentiment score scale mapped to a half-circle sweep:$$\text{transform: rotate(} (\text{score} \times 1.8) - 90 \text{deg)}$$Ensure the needle element features a high-contrast style (e.g., a solid neon purple stroke or a clean white vector with a subtle glow) so it is vividly visible against the dark translucent purple glass track.B. Implement the Strict 3-State Label LogicReplace the legacy descriptive texts or numeric limits on the right side of the card with exactly three hardcoded baseline states matching the original layout style:Max State Indicator: Apply a clean, low-glow greenish text class (e.g., text-emerald-400 or custom green glow) displaying a "Risk On" label.Mid State Indicator: Apply a sharp, balanced yellowish text class (e.g., text-amber-400) displaying a "Balanced" label.Min State Indicator: Apply a crisp, high-visibility red warning text class (e.g., text-rose-500) displaying a "Risk Off" label.C. Absolute Gauge Face Stripping & DOM PurgeZero Numbers: Inspect the gauge sub-component rendering logic and verify that all surrounding scale numbers ($0, 25, 50, 75, 100$) are fully stripped out of the active SVG text tags. The arc track must remain entirely clean.Prune Duplicate Wrappers: Clean out any secondary layout containers or residual paddings added during the sizing phase that cause text alignments to mismatch with neighboring panels. The layout must be lean, flat, and single-layered.Windsurf Execution Checklist:Is the gauge needle completely visible and pivoting accurately from the bottom-center anchor point?Are all superficial numbers and tick rings eliminated from the visual gauge path?Are the "Risk On" (Greenish), "Balanced" (Yellowish), and "Risk Off" (Red) state blocks cleanly mapped on the layout?Has the underlying code structure been audited to drop all ghost nodes and unrendered divs?Go.